import { ALBUM_SELECTED } from '../utils/Constants';

const TagReducer = (tagName = '', action) => {
  switch (action.type) {
    case ALBUM_SELECTED:

      break;
    default:
      
  }
};

export default TagReducer;
